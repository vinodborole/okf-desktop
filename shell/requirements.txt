# Desktop shell. okf-kit[serve] provides the API the shell talks to.
okf-kit[serve,chat]>=0.3.3
pywebview>=5.0
# pywebview needs a platform webview backend:
#   Linux:   pip install pywebview[qt]     (or [gtk] with system GTK/WebKit2)
#   Windows: uses the built-in Edge WebView2 (no extra install)
#   macOS:   uses the built-in WKWebView (no extra install)
